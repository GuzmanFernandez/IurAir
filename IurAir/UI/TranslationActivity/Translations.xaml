<Page x:Class="IurAir.UI.TranslationActivity.Translations"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IurAir.UI.TranslationActivity" xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Background="WhiteSmoke"
      Title="Translations">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="VisibilityConverter"/>
    </Page.Resources>
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="Black">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Viewbox Grid.Column="1" Margin="8">
                <Button x:Name="SetupBTN" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" BorderThickness="0"  Click="Setup">
                    <Image Source="{StaticResource GearWhite}" Stretch="Uniform"/>
                </Button>
            </Viewbox>

            <Viewbox Grid.Column="0" Margin="8">
                <Button 
                    Command="{Binding LoadFile}"
                    x:Name="TranslateBTN" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Stretch"
                    Background="Transparent" 
                    BorderThickness="0">
                    <Image Source="{StaticResource TranslateSelected}" Stretch="Uniform"/>
                </Button>
            </Viewbox>
        </Grid>
        <TabControl Grid.Row="1" Visibility="{Binding VisibleLoader, Converter={StaticResource VisibilityConverter}}">
            <TabItem Header="Single Translation">
                <Grid Margin="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="10*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="14*"/>
                    </Grid.RowDefinitions>
                    <GroupBox Header="Load file" Grid.Column="0" MaxWidth="80" HorizontalAlignment="Left">
                        <Button x:Name="LoadBTN" HorizontalAlignment="Left" VerticalAlignment="Stretch" Background="Transparent" BorderThickness="0" 
                                Command="{Binding LoadFile}">
                            <Image Source="{StaticResource FileImport}" Stretch="Uniform"/>
                        </Button>
                    </GroupBox>
                    <GroupBox Grid.Column="1" Header="File">
                        <TextBox MaxHeight="30" VerticalAlignment="Center" HorizontalAlignment="Left" Text="{Binding CurrentFile, Mode=OneWay}" IsReadOnly="True">
                            <behaviors:Interaction.Triggers>
                                <behaviors:EventTrigger EventName="PreviewMouseDown">
                                    <behaviors:InvokeCommandAction Command="{Binding OpenFolder}"/>
                                </behaviors:EventTrigger>
                            </behaviors:Interaction.Triggers>
                        </TextBox>
                    </GroupBox>
                    <Grid Grid.Row="1" Grid.ColumnSpan="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <GroupBox Header="Sabre" Grid.Column="0">
                            <ScrollViewer HorizontalScrollBarVisibility="Auto">
                                <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left">
                                    <TextBox IsReadOnly="True" BorderThickness="0" Margin="0,0,0,20" Text="{Binding IurFile, Mode=OneWay}"/>
                                </StackPanel>
                            </ScrollViewer>
                        </GroupBox>
                        <GroupBox Header="Amadeus" Grid.Column="1">
                            <ScrollViewer HorizontalScrollBarVisibility="Auto">
                                <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left">
                                    <TextBox IsReadOnly="True" BorderThickness="0" Margin="0,0,0,20" Text="{Binding AirFile, Mode=OneWay}"/>
                                </StackPanel>
                            </ScrollViewer>
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Bulk Translation">

            </TabItem>
            <TabItem Header="Utilities">
                
            </TabItem>
        </TabControl>


        <TabControl Grid.Row="1" Visibility="{Binding VisibleDetails, Converter={StaticResource VisibilityConverter}}">
            <TabItem Header="Details">
                <ScrollViewer HorizontalScrollBarVisibility="Auto">
                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left">
                        <Grid>
                            <DataGrid Name="Passengers" ItemsSource="{Binding Passengers}" AutoGenerateColumns="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="#" Binding="{Binding InterfaceNr}"/>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                                    <DataGridTextColumn Header="Type" Binding="{Binding Category}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>

                        <Grid>
                            <DataGrid Name="Itinerary" ItemsSource="{Binding Itinerary}" AutoGenerateColumns="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="#" Binding="{Binding InterfaceNr}"/>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                                    <DataGridTextColumn Header="Type" Binding="{Binding Category}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="File explanation">

            </TabItem>
            
        </TabControl>
        <Grid Grid.Row="2" Visibility="{Binding DocumentLoaded, Converter={StaticResource VisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="11*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Visibility="{Binding VisibleLoader, Converter={StaticResource VisibilityConverter}}" Grid.Column="0" HorizontalAlignment="Center" Text="Details" Grid.Row="0"/>
            <Viewbox Visibility="{Binding VisibleLoader, Converter={StaticResource VisibilityConverter}}"  Grid.Column="0" HorizontalAlignment="Center" Grid.Row="1" >
                <Button x:Name="DetailsBTN" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" BorderThickness="0"
                            Command="{Binding ShowDetails}">
                    <Image Source="{StaticResource Plane}" Stretch="Uniform"/>
                </Button>
            </Viewbox>
            <TextBlock Visibility="{Binding VisibleDetails, Converter={StaticResource VisibilityConverter}}" Grid.Column="0" HorizontalAlignment="Center" Text="File Viewer" Grid.Row="0"/>
            <Viewbox Visibility="{Binding VisibleDetails, Converter={StaticResource VisibilityConverter}}" Grid.Column="0" HorizontalAlignment="Center" Grid.Row="1">
                <Button x:Name="LoaderBTN" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" BorderThickness="0"
                            Command="{Binding ShowDetails}">
                    <Image Source="{StaticResource Upload}" Stretch="Uniform"/>
                </Button>
            </Viewbox>

            <Viewbox Grid.Column="1" HorizontalAlignment="Right" Grid.Row="1" Margin="0,0,20,0">
                <Button x:Name="DownloadBTN" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" BorderThickness="0"
                            Command="{Binding Download}">
                    <Image Source="{StaticResource Export}" Stretch="Uniform"/>
                </Button>
            </Viewbox>

        </Grid>
    </Grid>
</Page>
